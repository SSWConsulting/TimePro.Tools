# 0.3.6

Command line
- `tp whats-new` shows the current version's notes instead of "No local release notes were found for this version range." This mostly affected anyone running more than one version of `tp`, for example while developing or testing, where an older build ran after a newer one.
