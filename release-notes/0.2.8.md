# 0.2.8

- Added `--less <MINUTES>` to `tp ts update`, so deducted time can be changed
  without deleting and recreating the timesheet. Pass `--less 0` to clear it.
- Negative `--less` values now fail before TimePro is updated.
