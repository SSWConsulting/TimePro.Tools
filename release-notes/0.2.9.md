# 0.2.9

- Fixed `tp ts update` for timesheets on projects that use iterations. Version
  0.2.8 could fail when TimePro returned an iteration name without its numeric
  ID; updates now resolve and preserve the correct iteration before saving.
- Project changes now resolve the iteration against the target project and stop
  before saving if it cannot be matched safely.
